<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp">

    <ImageView
        android:id="@+id/friendImageView"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_marginEnd="12dp"
        android:contentDescription="@string/friend_image_description" />

    <TextView
        android:id="@+id/friendNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="Roboto-Regular"
        android:maxLines="1"
        android:textColor="@color/friendNameTextColor"
        android:textSize="16sp"
        android:layout_toEndOf="@id/friendImageView"/>

    <TextView
        android:id="@+id/friendIsOnlineTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/friendNameTextView"
        android:layout_alignStart="@+id/friendNameTextView"
        android:fontFamily="Roboto-Regular"
        android:textSize="14sp"
        android:textColor="@color/friendIsOnlineTextColor"/>

</RelativeLayout>